<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Engedélyek -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BicycleStorage"
        tools:targetApi="31"
        android:usesCleartextTraffic="false">

        <!-- ÚJ: Bejelentkezés a belépési pont (LAUNCHER) -->
        <activity
            android:name=".auth.LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize|stateHidden"
            tools:targetApi="31">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Regisztráció (appon belülről hívható) -->
        <activity
            android:name=".auth.RegisterActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize|stateHidden" />

        <!-- Fiók (adat módosítás / kijelentkezés) -->
        <activity
            android:name=".auth.AccountActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize|stateHidden" />

        <!-- Fő térkép képernyő – NEM LAUNCHER többé -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            tools:ignore="MissingClass" />

        <!-- Google Maps API kulcs (JAVASOLT: korlátozd csomagnév + SHA-1 alapján!) -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAkzbLz06eOFc5MNG8hRX9C5pOIjOL6mMQ" />

    </application>

</manifest>